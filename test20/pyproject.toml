[project]
name = "test20"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "cowsay>=6.1",
    "pyspark>=3.5.5",
    "pytest>=8.3.5",
]

[project.scripts]
main = "test20.main:main"

 
