targets:
  # The 'dev' target, used for development purposes.
  # Whenever a developer deploys using 'dev', they get their own copy.
  dev:
    # We use 'mode: development' to make sure everything deployed to this target gets a prefix
    # like '[dev my_user_name]'. Setting this mode also disables any schedules and
    # automatic triggers for jobs and enables the 'development' mode for Delta Live Tables pipelines.
    mode: development
    default: true
    workspace:
      host: https://adb-3112359112064034.14.azuredatabricks.net/
    variables:
      warehouse_id:
        lookup:
          # by default sql warehouse is sql-warehouse
          # if pro warehouse is needed then sql-warehouse-pro
          # type is not provided
          # sql-warehouse (this is serverless)
          warehouse: warehouse-data-services-dev # sql-warehouse
